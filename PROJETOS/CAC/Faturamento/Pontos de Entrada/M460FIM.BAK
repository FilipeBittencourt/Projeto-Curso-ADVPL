User function M460FIM()
            //COMECEI A FAZER, NÃO ESTA PRONTO
			SE1->(DbSetOrder(2))           
			cChaveE1 := XFilial("SE1")+SF2->F2_CLIENTE+SF2->F2_LOJA+SF2->F2_PREFIXO+SF2->F2_DOC
			IF SE1->(DbSeek(cChaveE1))
			
		    	While .Not. SE1->(Eof()) .And. SE1->(E1_FILIAL+E1_CLIENTE+E1_LOJA+E1_PREFIXO+E1_NUM) == cChaveE1
		    	                      
		    	    if SE1->E1_MULTNAT <> '1'
			    	    RecLock("SE1",.F.)
			    	    SE1->E1_MULTNAT := '1'
			    	    SE1->(MSUnlock())
			        endif      
			        
		    		SE1->(DbSkip())
		    	enddo
			
			ENDIF
		
			nTotDup
		endif

Return