#include "rwmake.ch"        // incluido pelo assistente de conversao do AP5 IDE em 29/01/01

// Fonte Descontinuado - MVC_ITEM

/*

User Function Mta010ok()        // incluido pelo assistente de conversao do AP5 IDE em 29/01/01
SetPrvt("WALIAS,")

wAlias := Alias()

dbSelectArea("SB5")
dbSetOrder(1)
If DbSeek(xFilial("SB5")+SB1->B1_COD)
   Reclock("SB5",.f.)
   Delete
   MsUnlock()
EndIf

dbSelectArea(wAlias)

Return(.T.)
*/